Zabon v3.2: Strict Lesson Generation Prompt (Category Lessons Only) Objective: Generate a structurally perfect lesson JSON file that passes semantic and cohesion checks. Tokenization for unsegmented languages is handled by a post-processing script. ⚠️ CRITICAL CAVEAT: We are processing ONLY lessons defined under `"categories"` in `manifest.json`. Ignore any lessons defined under `"topics"`. Target File: `lessons/grammar/[LESSON_FILENAME].json` (e.g., `universal-passive.json`) Proficiency: [Specify: beginner (25+ sentences), intermediate (35+ sentences), or advanced (50+ sentences)] 🚨 CRITICAL RULE: Sentence-First Extraction Method To guarantee a 100% pass on the validator, you MUST NOT generate words and sentences independently. You must follow this exact order: 1. Write the sentences FIRST. Ensure every sentence has perfect translations for all 7 languages. 2. Extract the `words` DIRECTLY from the sentences. Do NOT use dictionary/infinitive forms. The `word` text for every language must be an exact substring of the text in at least one sentence. 📋 Step-by-Step Generation Instructions Step 1: Define Scenarios Create distinct, realistic scenarios relevant to the lesson topic. Ensure the total number of `kind: "sentence"` items meets the proficiency threshold (Beginner: 25, Intermediate: 35, Advanced: 50). Step 2: Generate Sentence Items (`kind: "sentence"`) For every sentence, provide: - `id`: e.g., `s1`, `s2`... - `kind`: `"sentence"` - `texts`: Translations for all 7 languages (`en`, `th`, `fa`, `ar`, `es`, `zh`, `ja`). ⚠️ STRICT RULE: Do not mix languages in texts. ⚠️ STRICT RULE: Japanese (`ja`) and Chinese (`zh`) MUST use full-width punctuation (`？`, `。`). NEVER use the Arabic question mark (`؟`) in `ja` or `zh`. - 🚫 **NO TRAILING/LEADING SPACES IN STRINGS:** Never include spaces inside the translation strings or at the beginning/end of the text. (e.g., `"zh": "开门。"` is correct; `"zh": " 开门。 "` is FATAL). Ensure all strings are tightly trimmed and contain no hidden whitespace. 🚫 DO NOT generate the `tokens` array for `th`, `zh`, and `ja`. A post-processing script will handle segmentation. Step 3: Extract Word Items (`kind: "word"`) After writing the sentences, extract the vocabulary. For every `word` item: - `id`: e.g., `w1`, `w2`... - `kind`: `"word"` - `texts`: Translations for all 7 languages. ⚠️ STRICT COHESION RULE: The string in `texts[lang]` MUST appear exactly as an exact substring (ignoring punctuation) in at least one sentence's `texts[lang]`. ⚠️ NO DICTIONARY FORMS: If the Arabic sentence uses "أذهب" (I go), the Arabic word for "go" MUST be "أذهب", NOT "يذهب" (he goes). Apply this strictly to `ar`, `fa`, `es`, and `ja`. 🚫 Absolute Prohibitions (Will Cause Immediate Failure) - NO Arabic question marks (`؟`) in Japanese or Chinese. Use `？` exclusively for `ja` and `zh`. - NO dictionary/infinitive forms in `word` items if the sentence uses a conjugated form. - NO missing languages. Every `word` and `sentence` must have all 7 languages (`en`, `th`, `fa`, `ar`, `es`, `zh`, `ja`). - NO cross-language contamination. ✅ Pre-Flight Checklist (Mentally verify before outputting) [ ] Is every `word.texts[lang]` an exact substring of at least one `sentence.texts[lang]`? [ ] Are there exactly 7 languages for every single item? [ ] Is the sentence count >= the proficiency threshold? [ ] Did you OMIT the `tokens` array for `th`, `zh`, and `ja`? 📤 Output Format Output ONLY the valid JSON block. Do not include markdown explanations, apologies, or conversational text before or after the JSON. { "version": 2, "items": [ { "id": "h1", "header": true, "texts": { "en": "Scenario 1: [Scenario Name]", "th": "...", "fa": "...", "ar": "...", "es": "...", "zh": "...", "ja": "..." } }, { "id": "s1", "kind": "sentence", "texts": { "en": "...", "th": "...", "fa": "...", "ar": "...", "es": "...", "zh": "...", "ja": "..." } }, { "id": "w1", "kind": "word", "texts": { "en": "...", "th": "...", "fa": "...", "ar": "...", "es": "...", "zh": "...", "ja": "..." } } ] }
