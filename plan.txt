"Update Existing Document" renders the "Edit Document Content" details panel that contains 
 nested blocks.  

There is a problem with rendering nested blocks as the content becomes progressivly narrow with each 
nested block.

Change the block rendition so that each block is rendered seperately in an HTML details element set to close by default. 
The block details elements should not be nested!

