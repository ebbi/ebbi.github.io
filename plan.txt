
The App is partly an aid for my school text for learning Thai and it generally works well for 
vocabulary, sentences and paragraphs with useful multiple choice quiz, flash card and constructing sentences.

The uploaded PDF text is an exception as it tries to teach Thai alphabet, vowels and basic spelling and reading.

Please advise with an overview as to how the uploaded text could be incorporated in the App.  


Excellent! 

I should have mentioned, this is a mobile first App by design.

With that in mind, can you reconsider your advice, particularly with point 
5. New Interactive Exercise: Character Grid and the 
Create a drag-and-drop matching interface: exercise.

A click based exercise may be more suitable.

After that please give detail instructions for updating the current App.


For point 1 - Record or source audio files. 

Can the exisitng App TTS be used instead?

=======================================

Excellent. 

I am happy with no further queries.  

In the current chat, there has been several updates to the gegenerated code.  Rather than trying to merge the replies, please generate the instruction to actually 
start editing the current code base.

===================================

Code is updated and ready for data.

The folder and naming convention used is as follows:

The file for the uploaded PDF text should be:
data/TSL/RW1.json 
 Each entry in the Content page is in a separate section definition

Please avoid comments in the JSON data file, it causes an error

I would like to keep all files in data/TSL 
 so, any other data file should be in this folder

Please generate the manifest.json and the JSON data files.

=========================================

The data in "type": "explanation" should be rendered in an HTML details element with default closed.

Click event on a consonant returns the error: 

Uncaught ReferenceError: Services is not defined
    onclick http://127.0.0.1:8080/#doc/RW1:1
127.0.0.1:8080:1:1
    onclick http://127.0.0.1:8080/#doc/RW1:1

==================================

Testing:
http://127.0.0.1:8080/#doc/RW1

But that is the Bug!

Clicking on an element does start playing from that element BUT 
it then misses playing some elements.  It should not! It should play all element from the selected element onwards.

Is it a timing issue? 


===============================

The race condition only occurs after several click events and then aligns iteself.

It is almost useable.

Can the click event be disabled during play?
(a message can be displayed during play, ... Stop play before Clicking an elemen)

Then the user either click an element Or 
the Play button but 
no click during Play.

That would be full proof and may simplify the code.

(a message can be displayed during play, ... Stop play before Clicking an elemen)


Notification appears after an element is clicked! 

The notification should also appear after the Play button click event. 

Also, it should remain for 5 seconds.


Old messages are being displayed!
The messages:
"stopped_for_click": "Stopped playback for clicked item",
    "playback_started": "Playback started - click any element to stop and play",
    "playback_stopped": "Playback stopped"

should be deleted.

The only messages necessary is fter the Play button or an element to play, with the message:

"Click Stop before clicking an item to play"


The notification message is still not rendered after clicking the Play button.

The notification message is still not rendered after clicking an element for the first time but 
it is rendered after a second click onward.





























