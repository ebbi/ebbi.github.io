// config.js
// ---------------------------------------------------------------
// Global configuration – values that can be imported anywhere.
// ---------------------------------------------------------------

// The base URL for the SPA.  When the app is served from the root
// (as in the demo) it is an empty string.  If you ever host the
// app under a sub‑folder, change this value accordingly.
export const BASE_URL = '';   // <-- adjust only if you need a prefix

// You can also expose other globals here later (e.g. API keys).